<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="
      http://www.springframework.org/schema/beans
      http://www.springframework.org/schema/beans/spring-beans.xsd
      http://www.springframework.org/schema/security
      http://www.springframework.org/schema/security/spring-security.xsd">

   <!-- context:property-placeholder location="classpath:oasis-datacore-security.properties,classpath:oasis-datacore-rest-api.properties,classpath:oasis-datacore-rest-server.properties,classpath*:oasis-datacore-deploy*.properties"
      order="-1" ignore-unresolvable="true"/ -->
	<import resource="classpath:oasis-datacore-security-oauth2-context.xml" />

   <!-- login entry point is Kernel's -->
	<bean id="oauthAuthenticationEntryPoint" class="org.springframework.security.web.authentication.LoginUrlAuthenticationEntryPoint">
		<constructor-arg name="loginFormUrl" value="${datacorePlayground.loginUrl}" />
	</bean>

   <!-- Spring Security web filterChain : -->
	<!-- NB. could maybe be overriden (name="org.springframework.security.filterChains") but not its filters  -->
	<security:http name="org.springframework.security.filterChains"
				   entry-point-ref="oauthAuthenticationEntryPoint"
				   access-decision-manager-ref="accessDecisionManager"
				   authentication-manager-ref="authenticationManager"
				   create-session="never"
				   use-expressions="true">

		<!-- Datacore status : -->
		<security:intercept-url pattern="/dc/status*" access="isAnonymous()" />

	   <!-- Datacore playground OASIS Kernel OAuth2 auth : -->
		<security:intercept-url pattern="/dc/playground/login*" access="isAnonymous()" />
		<security:intercept-url pattern="/dc/playground/token*" access="isAnonymous()" />
		<security:intercept-url pattern="/dc/playground/configuration*" access="isAnonymous()" /><!-- so configuration can be read by playground -->
		
		
		<!-- TODO LATER DEVMODE ONLY Datacore playground UI anonymous access :
		else 401 : undefined http://localhost:8080/api-docs -->
		<!-- NB. /dc/playground/configuration is called with auth (from ajax) even in devmode -->
		<security:intercept-url pattern="/api-docs/**" access="isAnonymous()" /><!-- TODO LATER rather hack swagger to call with auth (from ajax), or add cookie check auth filter instead #47 -->
		<security:intercept-url pattern="/dc-ui/**" access="isAnonymous()" /><!-- TOOO LATER rather add cookie check auth filter instead #47 -->
		
		<!-- Datacore protection : -->
		<security:intercept-url pattern="/**" access="hasRole('SCOPE_DATACORE') and isFullyAuthenticated()" /><!-- OUTSIDE DEVMODE protect everything -->

		<security:custom-filter ref="oauth2ProviderFilter" before="PRE_AUTH_FILTER" />
		<security:access-denied-handler ref="oauthAccessDeniedHandler" />
	</security:http>
	
</beans>
